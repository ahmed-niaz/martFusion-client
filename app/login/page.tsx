import LoginForm from "@/components/modules/auth/login/LoginForm";

const Login = () => {
  return (
    <section className="h-screen w-full mx-auto lg:w-screen flex justify-center items-center">
      <LoginForm />
    </section>
  );
};

export default Login;
